package Ejerciciospag70;

import java.util.Scanner;

public class Main {

    public static void main(String[] args) {
        // Ejercicio 1: un programa que permita almacenar cualquier valor numérico hasta
        // que el usuario decida de-
        // tener el programa, se debe calcular el promedio de los números introducidos y
        // la cantidad de números positivos.
       /*  Scanner entrada = new Scanner(System.in);
        int contador = 0;
        int positivos = 0;
        double suma = 0;
        String opcion;

        do {
            System.out.print("Ingresa un número: ");
            double numero = entrada.nextDouble();
            suma += numero;
            contador++;

            if (numero > 0) {
                positivos++;
            }

            System.out.print("¿Deseas ingresar otro número? (si/no): ");
            opcion = entrada.next();
        } while (opcion.equalsIgnoreCase("si"));

        if (contador > 0) {
            double promedio = suma / contador;
            System.out.println("Promedio: " + promedio);
            System.out.println("Cantidad de números positivos: " + positivos);
        } else {
            System.out.println("No se ingresaron números.");
        }*/

        // Ejercicio 2: un programa que lea una cadena y la invierta.
    }
}
